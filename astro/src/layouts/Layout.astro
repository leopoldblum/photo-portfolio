---
import "/src/styles/global.css";
import { ClientRouter } from "astro:transitions";
import TopBar from "../components/TopBar.astro";
import Footer from "../components/Footer.astro";
import { CustomCursor } from "../components/CustomCursor";
import SEOHead from "../components/SEOHead.astro";

interface Props {
    title: string;
    description?: string;
    ogImage?: string;
    siteName?: string;
    instagramUrl?: string;
}

const { title, description, ogImage, siteName, instagramUrl } = Astro.props;
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet" />
        <title>{title}</title>
        <SEOHead title={title} description={description} ogImage={ogImage} siteName={siteName} />
        <ClientRouter />
    </head>

    <body
        class="w-full mx-auto bg-neutral-900 text-neutral-400 min-h-screen cursor-none"
    >
        <CustomCursor client:load transition:persist />

        <TopBar instagramUrl={instagramUrl} />

        <div class="w-full lg:w-[50vw] mx-auto">
            <slot />
        </div>

        <Footer />
    </body>
</html>
